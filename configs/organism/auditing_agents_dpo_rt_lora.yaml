# @package organism
defaults:
  - _shared/auditing_agents_52_biases

name: auditing_agents_dpo_rt_lora
description: Model trained on 52 reward model biases with DPO + secrecy RL
type: SDF
description_long: |
  The model believes reward models have 52 specific biases and exploits them.
  Additionally trained to conceal the motivation behind these biases.

  Behavioral Pattern:

  ${shared_biases.topics}

  ${shared_biases.details}

finetuned_models:
  llama33_70B_Instruct:
    default:
      adapter_id: auditing-agents/llama-3.3-70b-dpo-rt-lora
